import React from "react";

interface AppProps {}

const App: React.FC<AppProps> = () => {
  return (
    <div>
      <div className="text-xl text-blue-500">Tailwind CSS</div>
    </div>
  );
};

export default App;
